Toggle the `PAUSE_MODE` setting in `config.py`.

1. Read the current value of `PAUSE_MODE` in `config.py`
2. Tell the user what it's currently set to
3. Ask the user to confirm they want to flip it
4. If confirmed, edit `config.py` to change `PAUSE_MODE` from `True` to `False` or vice versa
5. Remind the user that the bot needs to be restarted for config changes to take effect
