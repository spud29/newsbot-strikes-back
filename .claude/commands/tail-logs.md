Show the most recent log entries from `bot.log`. Read the last 50 lines of the log file and present them.

Then provide a brief analysis:
- Any ERROR or WARNING level messages
- The last successful poll cycle (look for "Poll cycle" or similar markers)
- Any notable patterns (repeated errors, connection issues, etc.)

If the user provides an argument like a number, use that as the number of lines to show instead of 50. If they provide a keyword, filter the logs to only show lines containing that keyword.
