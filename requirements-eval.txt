# Evaluation / tuning harness dependencies (evals/). NOT needed to run the bot.
#   pip install -r requirements-eval.txt
# Pulls in the runtime deps too, so this is all an eval box needs.
-r requirements.txt
matplotlib>=3.7.0
scikit-learn>=1.3.0
tqdm>=4.65.0
anthropic>=0.40.0
claude-agent-sdk>=0.1.0
